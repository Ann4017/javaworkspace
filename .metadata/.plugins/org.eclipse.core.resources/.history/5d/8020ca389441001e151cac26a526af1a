
public class CastingTest {

  public static void main(String[] args) {
    // 형변환(Casting)
    // boolean 제외
    // 타입을 다른 타입으로 변환하는 것
    // 자동형변환, 강제형변환
    // -------> 자동형변환
    // byte < short < int < long < float < double
    // char
    // 강제형변환 <-------

    int i = 30;
    byte b = (byte) i; // 강제형변환

    int i2 = 300;
    byte b2 = (byte) i2; // 값의 손실 발생

    byte b3 = 10;
    int i3 = b3; // 자동형변환

    // byte <-> char, short <-> char
    // 자동형변환 불가
    // 강제형변환 가능


  }

}
